# DEPRECATED (2026-04-18)
#
# 로컬 개발: `pnpm dev` 사용.
# 프로덕션 배포: Docker Swarm (`infra/docker-stack.app.yml`, CI/CD 자동화).
# 상세: docs/deploy.md
#
# 이 파일은 레거시 단독 docker-compose 배포 시절의 산물입니다.
# 로컬에서 이미지 빌드만 테스트하려면 아래 참조:
#
# docker build -t prod_nest:local .
# docker run --rm -p 3500:3500 --env-file .env prod_nest:local
